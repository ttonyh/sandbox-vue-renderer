const _ = require( 'lodash' ),
      run = require( 'parallel-webpack').run,
      wpConfig = require( './config/wp-parallel' ),
      options = {};

/*
    

function notify() {
    console.info( 'Notify: ', arguments );
}


run( wpConfig, options, notify );

*/
      

console.info( wpConfig );

